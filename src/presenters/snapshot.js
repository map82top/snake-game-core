export default class Snapshot {
    constructor(map, time, points, level, message) {
        this.map = map
        this.time = time
        this.points = points
        this.level = level
        this.message = message
    }
}